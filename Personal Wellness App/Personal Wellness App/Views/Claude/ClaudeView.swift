import SwiftUI

struct ClaudeView: View {
    var body: some View {
        NavigationStack {
            Text("Claude")
                .navigationTitle("Claude")
        }
    }
}

#Preview {
    ClaudeView()
}
