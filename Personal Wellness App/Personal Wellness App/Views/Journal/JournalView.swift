import SwiftUI

struct JournalView: View {
    var body: some View {
        NavigationStack {
            Text("Journal")
                .navigationTitle("Journal")
        }
    }
}

#Preview {
    JournalView()
}
